<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تعديل المقال</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css">
    <style>
        .container { max-width: 800px; margin: 2rem auto; }
        .back-link { margin-bottom: 1rem; display: inline-block; }
    </style>
</head>
<body>
    <div class="container">
        <a href="/weeklyletters" class="back-link">← العودة إلى جميع المقالات</a>
        <h1 class="mb-3">تعديل المقال</h1>

        <form action="/weeklyletters/<%= letter._id %>/edit" method="POST">
            <div class="mb-3">
                <label for="title" class="form-label">العنوان</label>
                <input id="title" name="title" class="form-control" value="<%= letter.title %>" required>
            </div>
            <div class="mb-3">
                <label for="body" class="form-label">المحتوى</label>
                <textarea id="body" name="body" class="form-control" rows="10" required><%= letter.body %></textarea>
            </div>
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">حفظ التغييرات</button>
            </div>
        </form>

        <form action="/weeklyletters/<%= letter._id %>/delete" method="POST" onsubmit="return confirm('هل أنت متأكد أنك تريد حذف هذا المقال؟');" style="display:inline-block; margin-top:1rem;">
            <button type="submit" class="btn btn-danger">حذف المقال</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>