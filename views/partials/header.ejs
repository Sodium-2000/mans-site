<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'موقع المقالات الأسبوعية' %></title>
    <link rel="stylesheet" href="/styles/theme.css">
    <!-- Bootstrap RTL CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css">
    
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <div class="container">
    <a class="navbar-brand" href="/">موقع محمد عبدالله سالم</a>

    <!-- Hamburger toggle button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible section -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link" href="/activities">الأنشطة والدورات</a></li>
        <li class="nav-item"><a class="nav-link" href="/weeklyletters">المقالات</a></li>
        <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
          <li class="nav-item"><a class="nav-link" href="/admin/logout">تسجيل الخروج</a></li>
        <% } else { %>
          <li class="nav-item"><a class="nav-link" href="/admin/login">دخول الإدارة</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

    <div class="container">
